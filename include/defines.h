#pragma once

#include <QString>

inline const QString bfsInfo =
    "<strong>Breadth-first search (BFS)</strong> is an algorithm for searching a tree data structure for a node that satisfies a given "
    "property. It starts at the tree root and explores all nodes at the present depth prior to moving on to the nodes at the next depth "
    "level. Extra memory, usually a queue, is needed to keep track of the child nodes that were encountered but not yet explored. Read "
    "more on <a href='https://en.wikipedia.org/wiki/Breadth-first_search'>wikipedia.</a>";

inline const QString dfsInfo =
    "<strong>Depth-first search (DFS)</strong> is an algorithm for traversing or searching tree or graph data structures. The algorithm "
    "starts at the root node (selecting some arbitrary node as the root node in the case of a graph) and explores as far as possible along "
    "each branch before backtracking. Read more on <a href='https://en.wikipedia.org/wiki/Depth-first_search'>wikipedia.</a>";

inline const QString dijkstraInfo = "<strong>Dijkstra's algorithm</strong> is an algorithm for finding the shortest paths between nodes in "
						"a graph, which may represent, for example, road networks. It follows a greedy approach. Read more on "
						"<a href ='https://en.wikipedia.org/wiki/Dijkstra%27s_algorithm'>wikipedia.</a>";